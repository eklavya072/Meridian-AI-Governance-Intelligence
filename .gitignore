# ── Environment & secrets (never commit) ────────────────────────────────
.env
.env.local
.env.prod
.env.*.local

# ── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Node / Next.js ──────────────────────────────────────────────────────
node_modules/
.next/
out/
npm-debug.log*
*.tsbuildinfo

# ── ChromaDB persistence (indexed corpus — shipped separately, never git) ─
**/chroma/

# ── Raw framework PDFs (large, sourced locally — shipped separately) ────
**/raw_policies/*.pdf
!**/raw_policies/.gitkeep

# ── Uploaded documents & generated output (private user data) ───────────
backend/data/uploads/
backend/data/reports/
backend/data/gemini_rpd.json

# ── IDE / OS ────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# ── Logs ────────────────────────────────────────────────────────────────
*.log

# ── Chroma incident backups (kept on disk for forensics, never git) ─────
backend/data/chroma_broken_*/
backend/data/chroma_hnsw_corrupt_*/

# ── Stray local SQLite (the app uses Postgres) ──────────────────────────
backend/data/*.db

# ── Coverage output ────────────────────────────────────────────────────
.coverage
coverage.xml
htmlcov/

# ── Editor backups ─────────────────────────────────────────────────────
*.bak

# ── Demo PDFs staged for the landing page (large, unreferenced by code) ─
# The DIRECTORY must stay tracked (.gitkeep): frontend/Dockerfile does
# COPY --from=builder /app/public ./public, which fails on a clean clone if
# nothing in public/ is committed.
frontend/public/*.pdf
